\# LegalProp AI: Diseño, Arquitectura y Prototipado (Pipeline RAG \+ FastAPI \+ Gemini API) Este Jupyter Notebook compila la especificación arquitectónica completa, el diseño bajo el patrón \*\*MVC\*\*, y la implementación funcional prototípica del pipeline \*\*RAG\*\* (Retrieval-Augmented Generation) con \*\*Gemini API\*\*, preparado para ejecutarse en Google Colab o en entorno local con Jupyter. \--- \#\# 1\. Instalación de Dependencias \`\`\`python \# Instalación de librerías base para backend FastAPI, embeddings, vector store y Gemini \!pip install \-q fastapi uvicorn google-genai chromadb pydantic python-dotenv \`\`\` \--- \#\# 2\. Resumen Ejecutivo y Especificación de Arquitectura \* \*\*Dominio:\*\* Asistente conversacional especializado en normativa de arriendos (comercial y vivienda urbana) y régimen de propiedad horizontal (mascotas, manuales de convivencia, cánones, expensas y causales de restitución/desalojo). \* \*\*Patrón Arquitectónico:\*\* \*\*MVC (Modelo-Vista-Controlador)\*\*: \* \*\*Vista (V):\*\* Single Page Application en React con Tailwind CSS (manejo de hilos conversacionales y streaming de respuestas). \* \*\*Controlador (C):\*\* Enrutadores asíncronos en FastAPI y orquestador del ciclo RAG. \* \*\*Modelo (M):\*\* Almacén relacional (sesiones e historiales) \+ Almacén Vectorial (ChromaDB / pgvector) con chunks normativos indexados. \* \*\*Pipeline RAG:\*\* Extracción jerárquica de leyes/reglamentos, embeddings semánticos, búsqueda de similitud y generación fundamentada (\*grounding\*) mediante \*\*Gemini API\*\*. \--- \#\# 3\. Modelo (Model): Estructuras de Datos y Base Vectorial \`\`\`python import os import chromadb from typing import List, Optional from pydantic import BaseModel, Field \# \--- Esquemas de Datos (DTOs / Pydantic) \--- class DocumentChunk(BaseModel): id: str content: str normativa: str articulo: str categoria: str class QueryRequest(BaseModel): session\_id: str question: str require\_web\_search: bool \= False class SourceReference(BaseModel): normativa: str articulo: str fragmento: str class QueryResponse(BaseModel): answer: str sources: List\[SourceReference\] model\_used: str \# \--- Inicialización del Almacén Vectorial en Memoria \--- chroma\_client \= chromadb.Client() collection \= chroma\_client.get\_or\_create\_collection(name="legalprop\_knowledge") print("Almacén vectorial inicializado exitosamente.") \`\`\` \--- \#\# 4\. Ingesta y Segmentación Documental (ETL de Conocimiento) \`\`\`python \# Base de conocimiento normativo prototípica para pruebas normativas\_corpus \= \[ { "id": "art\_mascotas\_01", "normativa": "Régimen de Propiedad Horizontal / Manual de Convivencia", "articulo": "Artículo 15: De la Tenencia de Animales Domésticos", "categoria": "mascotas", "texto": "Está permitida la tenencia de animales domésticos siempre que no alteren la salubridad y tranquilidad del edificio. Es obligatorio el uso de correa en zonas comunes. La copropiedad no puede prohibir de manera general la tenencia de mascotas, pero sí reglamentar su circulación." }, { "id": "art\_canon\_02", "normativa": "Régimen de Arrendamiento de Vivienda Urbana", "articulo": "Artículo 20: Incremento del Canon de Arrendamiento", "categoria": "arriendos", "texto": "Cada doce meses de ejecución del contrato bajo un mismo precio, el arrendador podrá incrementar el canon hasta en una proporción que no supere el 100% del incremento que haya tenido el Índice de Precios al Consumidor (IPC) en el año calendario inmediatamente anterior." }, { "id": "art\_desalojo\_03", "normativa": "Procedimiento de Restitución de Inmueble Arrendado", "articulo": "Artículo 22: Causales de Terminación y Restitución", "categoria": "desalojos", "texto": "El no pago de los cánones de arrendamiento dentro del término pactado constituye causal suficiente para la terminación unilateral del contrato y el inicio de la acción judicial de restitución de inmueble. Ningún arrendador puede ejecutar desalojos de hecho por mano propia." } \] \# Ingesta en ChromaDB for doc in normativas\_corpus: collection.add( documents=\[doc\["texto"\]\], metadatas=\[{ "normativa": doc\["normativa"\], "articulo": doc\["articulo"\], "categoria": doc\["categoria"\] }\], ids=\[doc\["id"\]\] ) print(f"Total de fragmentos normativos indexados: {collection.count()}") \`\`\` \--- \#\# 5\. Servicio de LLM e Integración con Gemini API \`\`\`python from google import genai from google.genai import types \# Configuración del cliente oficial de Gemini \# Nota: Configura tu API Key mediante variable de entorno GEMINI\_API\_KEY client \= genai.Client() def generate\_rag\_answer(user\_query: str, retrieved\_context: list) \-\> str: """ Ensambla el prompt aumentado y llama a Gemini garantizando cero alucinaciones """ context\_text \= "\\n\\n".join(\[ f"Norma: {item\['metadata'\]\['normativa'\]}\\n" f"Articulo: {item\['metadata'\]\['articulo'\]}\\n" f"Texto: {item\['document'\]}" for item in retrieved\_context \]) system\_instruction \= ( "Eres LegalProp AI, un asistente jurídico especializado en arriendos y propiedad horizontal. " "Debes responder de forma clara, concisa y veraz, basándote ÚNICAMENTE en las normativas provistas en el contexto. " "No extrapoles información ni inventes leyes. Si el contexto no contiene la respuesta, indícalo expresamente. " "Cita siempre el artículo y la norma correspondiente al fundamentar tu respuesta." ) prompt \= f"Contexto normativo recuperado:\\n{context\_text}\\n\\nPregunta del usuario: {user\_query}" response \= client.models.generate\_content( model="gemini-2.5-flash", contents=prompt, config=types.GenerateContentConfig( system\_instruction=system\_instruction, temperature=0.1 ) ) return response.text \`\`\` \--- \#\# 6\. Controlador (Controller): Orquestación del Flujo RAG \`\`\`python class LegalPropController: """ Controlador que recibe consultas de la Vista, consulta el Modelo (VectorDB) y orquesta la generación con el servicio de Gemini. """ @staticmethod def process\_query(req: QueryRequest) \-\> QueryResponse: \# 1\. Recuperación semántica en ChromaDB search\_results \= collection.query( query\_texts=\[req.question\], n\_results=2 ) retrieved\_docs \= \[\] source\_refs \= \[\] if search\_results and "documents" in search\_results: docs \= search\_results\["documents"\]\[0\] metas \= search\_results\["metadatas"\]\[0\] for doc\_text, meta in zip(docs, metas): retrieved\_docs.append({"document": doc\_text, "metadata": meta}) source\_refs.append(SourceReference( normativa=meta.get("normativa", ""), articulo=meta.get("articulo", ""), fragmento=doc\_text\[:120\] \+ "..." )) \# 2\. Generación aumentada mediante Gemini answer \= generate\_rag\_answer(req.question, retrieved\_docs) \# 3\. Retorno del objeto de respuesta tipado return QueryResponse( answer=answer, sources=source\_refs, model\_used="gemini-2.5-flash" ) print("Controlador RAG configurado y listo para ejecución.") \`\`\` \--- \#\# 7\. Ejecución de Pruebas Unitarias del Flujo \`\`\`python \# Prueba 1: Consulta sobre tenencia de mascotas sample\_query\_1 \= QueryRequest( session\_id="session\_test\_001", question="¿La administración del edificio me puede prohibir tener a mi perro dentro de mi apartamento?" ) response\_1 \= LegalPropController.process\_query(sample\_query\_1) print(f"--- PREGUNTA: {sample\_query\_1.question} \---") print(f"\\nRESPUESTA:\\n{response\_1.answer}\\n") print(f"FUENTES CITADAS: {\[s.articulo for s in response\_1.sources\]}") \`\`\` \`\`\`python \# Prueba 2: Consulta sobre incremento de canon de arriendo sample\_query\_2 \= QueryRequest( session\_id="session\_test\_002", question="¿Cuánto es lo máximo que me pueden subir el arriendo cada año?" ) response\_2 \= LegalPropController.process\_query(sample\_query\_2) print(f"--- PREGUNTA: {sample\_query\_2.question} \---") print(f"\\nRESPUESTA:\\n{response\_2.answer}\\n") print(f"FUENTES CITADAS: {\[s.articulo for s in response\_2.sources\]}") \`\`\` \--- \#\# 8\. Exportación de la API con FastAPI (Controlador REST) \`\`\`python from fastapi import FastAPI import uvicorn app \= FastAPI( title="LegalProp AI Backend API", description="API REST del controlador para consultas legales sobre arriendos y propiedad horizontal", version="1.0.0" ) @app.post("/api/v1/chat", response\_model=QueryResponse) async def chat\_endpoint(request: QueryRequest): """Endpoint principal de consulta conversacional.""" return LegalPropController.process\_query(request) @app.get("/api/v1/health") async def health\_check(): """Chequeo de estado del servicio.""" return {"status": "ok", "indexed\_chunks": collection.count()} \# Para arrancar el servidor en entorno local: \# uvicorn.run(app, host="0.0.0.0", port=8000) \`\`\`